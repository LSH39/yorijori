<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="stat">
  <select id="selectFreeCount" resultType="prev">
  
  select
   (select count(*) from freeboard where reg_date =to_char(sysdate,'yyyy-mm-dd'))today,
   (select count(*) from freeboard where reg_date =to_char(sysdate-1,'yyyy-mm-dd'))prev1,
   (select count(*) from freeboard where reg_date =to_char(sysdate-2,'yyyy-mm-dd'))prev2,
   (select count(*) from freeboard where reg_date =to_char(sysdate-3,'yyyy-mm-dd'))prev3,
   (select count(*) from freeboard where reg_date =to_char(sysdate-4,'yyyy-mm-dd'))prev4,
   (select count(*) from freeboard where reg_date =to_char(sysdate-5,'yyyy-mm-dd'))prev5,
   (select count(*) from freeboard where reg_date =to_char(sysdate-6,'yyyy-mm-dd'))prev6,
   (select count(*) from freeboard where reg_date =to_char(sysdate-7,'yyyy-mm-dd'))prev7
   
   from dual
  
  </select>
  
  <select id="selectFjr" parameterType="string" resultType="prev">
  select
   (select count(*) from freeboard where reg_date= #{today})today,
   (select count(*) from member where enroll_date= #{today})prev1,
   (select count(*) from recipe_board where recipe_date= #{today})prev2
   
   from dual
  
  
  </select>
</mapper>
